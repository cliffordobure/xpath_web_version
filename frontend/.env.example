# API base URL (used at build time). Leave unset for local dev (Vite proxies /api to backend).
# For production build pointing at the hosted backend:
VITE_API_URL=https://xpath-web-version.onrender.com/api
